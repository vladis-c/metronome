export const ARC_SIZE = 260;
export const ARC_RADIUS = ARC_SIZE / 2;
export const KNOB_SIZE = 24;
export const KNOB_PATH_OFFSET = -7.5;
export const TOUCH_EXTRA = 20;
/** Needed for knob to go to the limits visually */
export const ANGLE_EPSILON = Math.PI * 0.03;
